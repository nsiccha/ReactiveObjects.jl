# Gallery

A live demo of `@reactive` kernels — each card shows a small dependency
graph and a stepwise trace of which fields are valid / invalid as you
read and write properties. The em-dashes mark fields that have been
invalidated and not yet recomputed.

```@raw html
<div class="htmxo-embed" data-hx-base="live-reactiveobjects/gallery" hx-trigger="load" hx-swap="innerHTML">
  <em>Loading ReactiveObjects gallery…</em>
</div>
```
